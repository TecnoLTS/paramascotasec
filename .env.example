#############################################
# PARAMASCOTASEC - PLANTILLA DE VARIABLES
#############################################
#
# USO RECOMENDADO:
# - Produccion: usar este archivo como .env
# - Desarrollo: usar .env.development
#
# Comandos:
#   docker compose up -d --build
#   docker compose --env-file .env.development up -d --build
#
# PARAMETROS QUE PUEDEN VARIAR Y PARA QUE SIRVEN:
#
# APP_ENV
#   development -> levanta app-dev (npm run dev)
#   production  -> levanta app (npm run start)
APP_ENV=development

# COMPOSE_PROFILES
#   Perfil activo de docker compose.
#   Debe coincidir con APP_ENV.
COMPOSE_PROFILES=development

# PORT
#   Puerto publico del frontend en tu maquina.
PORT=3000

# APP_BIND_IP
#   En produccion conviene publicar el frontend solo en loopback
#   para forzar el paso por el gateway.
APP_BIND_IP=127.0.0.1

# APP_DEV_BIND_IP
#   En desarrollo permite exponer el dev server a la LAN.
APP_DEV_BIND_IP=0.0.0.0

# API_FETCH_TIMEOUT_MS
#   Tiempo maximo de espera en ms para llamadas frontend -> backend.
API_FETCH_TIMEOUT_MS=15000

# NEXT_PUBLIC_BASE_URL
#   URL base publica del frontend (visible en navegador).
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# NEXT_PUBLIC_BACKEND_URL
#   URL del backend usada por peticiones desde el navegador.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080/api

# NEXT_PUBLIC_AUTH_CSRF_COOKIE_NAME
#   Nombre de la cookie no-httpOnly usada para enviar cabecera CSRF en requests mutantes.
NEXT_PUBLIC_AUTH_CSRF_COOKIE_NAME=pm_csrf

# BACKEND_URL_INTERNAL
#   URL interna para peticiones servidor a servidor desde el contenedor de Next.js.
#   Mantener nombre de servicio Docker para evitar dependencia de IP.
BACKEND_URL_INTERNAL=http://paramascotasec-backend-web/api

# INTERNAL_PROXY_TOKEN
#   Secreto compartido para validar que solo el proxy interno de Next.js
#   pueda reenviar headers sensibles al backend.
INTERNAL_PROXY_TOKEN=replace-with-shared-internal-proxy-token

# PANEL_IP_MODE
#   off     -> sin restriccion IP para /my-account
#   private -> permite loopback + redes privadas RFC1918
#   custom  -> usa solo PANEL_IP_ALLOWLIST
PANEL_IP_MODE=private

# PANEL_IP_ALLOWLIST
#   Lista opcional de IPs/CIDR adicionales.
#   En modo private se suman a las privadas.
#   En modo custom se convierten en la lista efectiva.
PANEL_IP_ALLOWLIST=

# Ejemplo rapido para PRODUCCION:
# APP_ENV=production
# COMPOSE_PROFILES=production
# NEXT_PUBLIC_BASE_URL=https://paramascotasec.com
# NEXT_PUBLIC_BACKEND_URL=https://api.paramascotasec.com
